// Task: Code a function called 'statusOfKeys' that logs the keys parameter
function statusOfKeys(keys) {
  // Logs the 'keys' parameter to the console
  console.log(keys);
}

// Export the 'statusOfKeys' function to make it available for other files to use
module.exports = statusOfKeys;
